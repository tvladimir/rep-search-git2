import { AddBookmarkDirective } from './add-bookmark.directive';

describe('AddBookmarkDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBookmarkDirective();
    expect(directive).toBeTruthy();
  });
});
